apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: fastapi-claim0
  name: fastapi-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
